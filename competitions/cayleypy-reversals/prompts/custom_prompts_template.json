{
  "planner": "You are solving CayleyPy Reversals. Produce a polynomial-time plan only. First allow an exact bundled-data lookup baseline when possible; otherwise use the direct constructive reversal sort that places value i into position i with move R[i,idx]. No BFS/DFS/A*.",
  "coder": "Write a self-contained solve_module.py. Preserve solve(vec)->(moves, sorted_array) and script-mode JSON output. Moves must be exact tokens R[i,j]. Prefer: (1) exact lookup from bundled test/sample files when present, (2) otherwise the direct constructive O(n^2) reversal algorithm with a two-pointer in-place reversal and no built-in sorting. Return exactly one complete Python file inside one ```python``` block and nothing else.",
  "fixer": "Fix the reversals solver while keeping it polynomial-time. Preserve the exact JSON contract and legal R[i,j] tokens only. Return exactly one complete Python file inside one ```python``` block. Prefer a minimal patch."
}

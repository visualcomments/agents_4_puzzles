You are generating a Python solver for the Kaggle competition CayleyPy-pancake.

Task:
Given a permutation in test.csv, output a sequence of pancake prefix reversals that sorts it.
Data columns:
- test.csv: id,n,permutation
- submission.csv: id,permutation,solution

Move semantics:
- Legal move names are Rk where 2 <= k <= n.
- Rk reverses the first k elements of the current permutation.
- solution is a dot-separated path such as R4.R2.R5.
- Apply moves left-to-right.
- Target state is numeric ascending order of the permutation.

Scoring:
Minimize the total number of moves over all rows. Blank paths and UNSOLVED are not acceptable for a working solver.

Baseline artifacts available for improvement:
- competitions/cayleypy-pancake/baselines/pancake_baseline_solver.py
- competitions/cayleypy-pancake/baselines/baseline_submission.csv

Use this working baseline as a reference/fallback when the prompt asks for baseline improvement.
The baseline solves every row with legal dot-separated Rk prefix reversals and no UNSOLVED rows.
Any improvement must preserve full replay validity and reduce total move count.


Variant focus: Use baseline plus cheap neighbourhood/local search rewrites; do not require ML weights.

Implementation requirements:
- Provide solve(vec) -> (moves, sorted_array).
- moves must be list[str]; sorted_array must equal sorted(vec).
- Do not rely on internet access.
- Keep dependencies standard-library only.
- Include internal replay checks for candidate move sequences.
- If using the baseline, improve it by local rewrite/search rules only when they are proven by replay validation.

We are solving the Kaggle competition "CayleyPy 444 Cube Solve Optimally":
https://www.kaggle.com/competitions/cayley-py-444-cube

Write the code from scratch.
No reference baseline will be provided to the model for this prompt variant.
I need a Python module `solve_module.py` that keeps exact bundled-state solving and safely improves bundled move count.

PROMPT MODE MARKERS
- FROM_SCRATCH
- NO_BASELINE_PATCH_BIAS
- CREATIVE_SCORE_SEARCH

PRIORITIES
1. Correctness.
2. Lower bundled move count.
3. Deterministic polynomial-time behavior.

PUBLIC CONTRACT
- Keep `solve(vec) -> (moves, sorted_array)`.
- Keep script mode:
  `python solve_module.py "[1,2,3]"`
  -> `{"moves": [...], "sorted_array": [...]}`
- Use only official move names from `puzzle_info.json`.
- Do not invent moves.
- Do not rename generators internally.
- Apply moves left to right.
- `sorted_array` must be the real final state after replaying the returned moves.

COMPETITION FACTS
- The state vector length is 96.
- The solved state is `central_state` from `puzzle_info.json`.
- Submission paths are dot-separated move names.
- The Kaggle score is the total move count across bundled test states.
- `sample_submission.csv` is valid but non-optimal, so improving bundled paths is a valid strategy.

USE THE OFFICIAL BUNDLE
- Detect the competition data directory robustly.
- Load the official files at runtime.
- Do not inline or hardcode the contents of `puzzle_info.json`, `test.csv`, or `sample_submission.csv`.
- Preferred lookup order:
  1. `optimized_lookup.json` if present
  2. bundled optimized submission CSV if present
  3. rebuilt lookup from `test.csv` + `sample_submission.csv`
- Keep compatibility with the official competition bundle layout.

REQUIRED SOLVER BEHAVIOR
- Exact bundled lookup comes first.
- If `vec` matches a bundled test state, return a valid solving path for that exact state.
- For bundled states, do not return `UNSOLVED` if a valid bundled path exists.
- After lookup, run only safe deterministic path optimization.
- If a path is optimized, replay the optimized path and return the true final state.
- For bundled states, the final state must be the solved central state.
- For non-bundled inputs, a safe deterministic fallback is allowed.
- `UNSOLVED` is allowed only for truly unsupported non-bundled inputs.

ALLOWED IDEAS
- inverse cancellation and same-face reduction with the correct 4x4x4 cube face order
- commuting normalization derived only from official permutations
- fixed-radius exact replacement tables
- bounded local dynamic programming or fixed-window rewriting
- caching of permutations, composed effects, and optimized fragments
- a small fixed bank of deterministic optimizer variants, keeping the shortest valid result

FORBIDDEN
- relying on, patching, wrapping, or extending a baseline implementation
- unofficial move names or hidden aliases
- whole-state search
- BFS, DFS, A*, IDA*, beam search, or any search depth that grows with the instance
- invalid paths, partially validated paths, or speculative rewrites that can break bundled correctness

IMPLEMENTATION STYLE
- Self-contained module.
- Python standard library only.
- Practical runtime on the bundled competition rows.
- Clear helpers and exact replay-based validation.

STRICT OUTPUT REQUIREMENTS:
- Return exactly one JSON object and no prose outside JSON.
- The JSON object must contain exactly these keys: version, artifact_type, language, filename, code.
- Set version='code_response.v2', artifact_type='python_module', language='python', filename='solve_module.py'.
- Put the entire Python file only inside the code string.
- Do not wrap the code in markdown fences inside the JSON string.
- Do not include explanations, bullet points, extra keys, or commentary before or after the JSON object.
- Inside the code, omit comments and docstrings unless they are absolutely necessary.
- Preserve the public solve(vec) entrypoint and the script-mode JSON stdout contract with keys moves and sorted_array.
JSON EXAMPLE:
{
  "version": "code_response.v2",
  "artifact_type": "python_module",
  "language": "python",
  "filename": "solve_module.py",
  "code": "from __future__ import annotations\\n\\nimport json\\n\\n\\ndef solve(vec):\\n    return [], list(vec)\\n"
}
- The produced code must be self-contained and runnable.

## Success gate and anti-fallback requirements (added 2026-04-26)
- A round is successful only when you generate a real, importable solver script plus a valid non-empty Kaggle submission CSV. Fallback artifacts, copied `sample_submission.csv`, offline baseline placeholders, `UNSOLVED` rows, empty move strings, or provider-failure recovery outputs are failed attempts and must not be described as success.
- Keep the known-good exact lookup / exact replay path first. Then add only rollback-safe improvements: bounded exact local rewrites, deterministic candidate portfolios, semantic replay guards, and score-regression checks.
- Do not trade correctness for ambition. If a heuristic cannot be replay-verified on the official CayleyPy move algebra, reject it and keep the best verified candidate for that row.
- Preserve the required script contract: official legal moves only, deterministic output, compile/import cleanly, generate `submission.csv`, and keep stdout JSON parseable for the sweep pipeline.
- Never emit code that silently falls back to sample submissions, bundled baseline CSVs, or placeholder `UNSOLVED` records. If generation is uncertain, produce the smallest compile-valid solver with exact lookup and verified local optimizer rather than a broken large rewrite.



## 444-CUBE ADAPTATION GUARDS
- This is NOT a Megaminx solver. Do not assume 120 stickers, order-5 turns, Megaminx face ordering, or Megaminx-only rewrite rules.
- Use the official 4x4x4 bundle only: `competitions/cayley-py-444-cube/data/puzzle_info.json`, `test.csv`, and `sample_submission.csv`.
- State vectors have length 96 and the solved state is `central_state` from `puzzle_info.json`.
- Legal moves are exactly the keys under `generators` in `puzzle_info.json`; derive inverse pairs, commutation, and generator orders from official permutations.
- Kaggle submission schema is `initial_state_id,path`, with dot-separated legal move names in `path`.
- For bundled rows, exact lookup and exact replay validation must remain first; any optimizer may only promote a shorter path after replaying it to the official central state.

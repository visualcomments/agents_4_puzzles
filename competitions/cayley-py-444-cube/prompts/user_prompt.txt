We are solving the Kaggle competition "CayleyPy 444 Cube Solve Optimally".
Reference: https://www.kaggle.com/competitions/cayley-py-444-cube

PROBLEM SUMMARY
- State: a permutation/state vector of length 96.
- Goal: transform the input state into the central solved state from competitions/cayley-py-444-cube/data/puzzle_info.json.
- Legal moves are exactly the keys in puzzle_info.json (for example names like f0, -f0, r0, -r0, ...).
- Kaggle submissions use dot-separated move names in the path column.

INPUT / OUTPUT CONTRACT
- Implement solve(vec) -> (moves, sorted_array)
- moves must be either list[str] of legal generator names or the string "UNSOLVED"
- sorted_array must be the final state after applying the emitted moves
- Script mode must print JSON: {"moves": ..., "sorted_array": ...}

ALGORITHM REQUIREMENTS
- Polynomial time only.
- Do NOT use BFS, DFS, A*, IDA*, or unbounded search over the state graph.
- Constant-depth lookahead over the fixed generator set is allowed.
- Never invent move names; always use the keys from puzzle_info.json.

RECOMMENDED IMPLEMENTATION ORDER
1. If bundled test.csv + sample_submission.csv exist, build an exact lookup baseline:
       initial_state string -> valid path
   Return that exact path for exact matches.
2. For unseen states, load central_state + generators from puzzle_info.json and implement a bounded-lookahead greedy fallback:
   - apply_perm(state, perm)
   - apply_moves(state, moves)
   - score(state) with polynomial-time metrics such as matched positions / longest correct prefix / total mismatches
   - evaluate all legal 1-move candidates and optionally all 2-move macros
   - choose the best improving candidate repeatedly until solved or plateaued
3. If no progress is possible after a bounded number of iterations, return "UNSOLVED".

IMPLEMENTATION DETAILS
- Standard library only.
- Work on a copy of vec.
- Always ensure sorted_array equals the state produced by the emitted moves.
- Return exactly one complete Python file inside one ```python``` block.
- Do not add explanations outside the code block.

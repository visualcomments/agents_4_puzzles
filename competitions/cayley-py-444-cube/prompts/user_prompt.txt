You are generating a solver for Kaggle competition cayley-py-444-cube.

Competition facts from the official bundled files:
- Puzzle name: 4x4x4.
- Input file: competitions/cayley-py-444-cube/data/test.csv.
- Test rows: 1043.
- State column: initial_state.
- State vector length: 96.
- Solved target: central_state from puzzle_info.json, not sorted(range(n)).
- Generator count: 24.
- Legal moves are exactly the keys in puzzle_info.json generators:
  f0, -f0, f1, -f1, f2, -f2, f3, -f3, r0, -r0, r1, -r1, r2, -r2, r3, -r3, d0, -d0, d1, -d1, d2, -d2, d3, -d3
- Submission schema: initial_state_id,path.
- path is a dot-separated sequence of legal move names such as f1.-d3.r0.
- A valid path must transform the row initial_state to central_state under permutation convention new[i] = old[perm[i]].
- The objective is to minimize total move count while preserving validity.

Required Python contract:
- Create a complete solve_module.py.
- Standard library only.
- Keep solve(vec) -> (moves, sorted_array).
- moves may be list[str] or a dot-separated string, but every token must be a legal generator.
- The script mode must accept one JSON/list vector argument and print JSON with keys moves and sorted_array.
- Do not output UNSOLVED, blank paths, placeholder moves, copied prose, markdown, or sample-only artifacts.
- Do not hardcode the entire contents of puzzle_info.json, test.csv, or sample_submission.csv; read official bundled files at runtime when needed.
- It is allowed to build an exact lookup from the bundled test.csv and sample_submission.csv at runtime.
- For every returned path, replay it with official generators and return the true final state.
- For bundled rows, exact lookup and exact replay validation must happen before any optimization.
- Any optimization may be promoted only if it is shorter and replay-valid.

Strict 444-cube guards:
- This is a 4x4x4 cube task only.
- Do not assume any non-444-cube sticker count, face ordering, generator order, notation, or rewrite rules.
- Derive inverse pairs, generator orders, commutation, and local rewrite candidates from puzzle_info.json.
- Keep algorithms deterministic and bounded; no network, no Kaggle API calls, no external packages.

Output requirements for LLM code generation:
- Return exactly one JSON object and no prose outside JSON.
- JSON keys must be exactly: version, artifact_type, language, filename, code.
- version = code_response.v2; artifact_type = python_module; language = python; filename = solve_module.py.
- Put the full Python source only inside the code string.

Variant strategy: Regular
Start with a simple exact bundled lookup from test.csv to sample_submission.csv, then apply only safe cancellation and replay-verified local shortening.

Return exactly one JSON object:
{
  "version": "code_response.v2",
  "artifact_type": "python_module",
  "language": "python",
  "filename": "solve_module.py",
  "code": "from __future__ import annotations\n\nimport json\nimport sys\n\ndef solve(vec):\n    return [], list(vec)\n\nif __name__ == '__main__':\n    vec=json.loads(sys.argv[1])\n    moves,out=solve(vec)\n    print(json.dumps({'moves': moves, 'sorted_array': out}))\n"
}

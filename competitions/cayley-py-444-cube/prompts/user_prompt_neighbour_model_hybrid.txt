We are solving the Kaggle competition "CayleyPy 444 Cube Solve Optimally":
https://www.kaggle.com/competitions/cayley-py-444-cube

I need a Python module `solve_module.py` that stays fully compatible with the existing 4x4x4 cube repository pipeline and the new vendored neighbour-model hybrid lane.

ABSOLUTE REQUIREMENTS
- Preserve the public API `solve(vec) -> (moves, sorted_array)`.
- `vec` is a Python list[int].
- `moves` must be a list[str] of legal official move names from `puzzle_info.json`.
- `sorted_array` must be the actual final array obtained by replaying `moves` from `vec`.
- Script mode must print JSON: `{"moves": [...], "sorted_array": [...]}`.
- Use ONLY official move names from the bundled 4x4x4 cube competition data.
- Keep exact bundled-state lookup support and do not regress correctness.

NEIGHBOUR-MODEL HYBRID CONTEXT YOU MUST RESPECT
This repository now includes a vendored 4x4x4 cube neighbour-model training snapshot and a runtime lane that can solve selected hard rows from scratch with a Q-model. Your generated solver must remain compatible with that workflow.

Concretely:
- do not invent new move aliases or hidden macro tokens;
- do not change CSV / JSON contracts used by hybrid candidate fusion;
- do not remove the exact lookup first architecture;
- keep deterministic behavior so LLM-generated candidates can still be fused safely with neighbour-model candidates;
- prefer improvements that strengthen local exact rewrites, canonicalization, and bounded dynamic programming around valid words.

STRATEGIC DIRECTION
Keep the baseline as an exact bundled lookup + 4x4x4 cube word-rewriting engine.
The neighbour-model lane already covers the "solve selected rows from scratch" role, so your job is to improve the exact deterministic solver layer without breaking interoperability.

Good directions:
- stronger short exact replacement tables;
- safer canonicalization of commuting blocks;
- better left-to-right / right-to-left bounded local optimization;
- memoization of local permutation effects;
- preserving the shortest representative for each local exact effect;
- cleaner factoring that makes the solver easier for the hybrid orchestrator to reuse.

Avoid:
- replacing exact lookup with generic search;
- introducing non-official move syntax;
- changing the solve contract;
- anything that would make neighbour-model + LLM candidate fusion less reliable.

OUTPUT EXPECTATION
Return only a complete `solve_module.py` implementation that improves the deterministic 4x4x4 cube baseline while staying compatible with the new neighbour-model hybrid lane and the existing prompt-driven LLM workflow.

## Success gate and anti-fallback requirements (added 2026-04-26)
- A round is successful only when you generate a real, importable solver script plus a valid non-empty Kaggle submission CSV. Fallback artifacts, copied `sample_submission.csv`, offline baseline placeholders, `UNSOLVED` rows, empty move strings, or provider-failure recovery outputs are failed attempts and must not be described as success.
- Keep the known-good exact lookup / exact replay path first. Then add only rollback-safe improvements: bounded exact local rewrites, deterministic candidate portfolios, semantic replay guards, and score-regression checks.
- Do not trade correctness for ambition. If a heuristic cannot be replay-verified on the official CayleyPy move algebra, reject it and keep the best verified candidate for that row.
- Preserve the required script contract: official legal moves only, deterministic output, compile/import cleanly, generate `submission.csv`, and keep stdout JSON parseable for the sweep pipeline.
- Never emit code that silently falls back to sample submissions, bundled baseline CSVs, or placeholder `UNSOLVED` records. If generation is uncertain, produce the smallest compile-valid solver with exact lookup and verified local optimizer rather than a broken large rewrite.


You are solving the Kaggle CayleyPy 4x4x4 cube competition by generating a real deterministic Python solver script.

Primary objective: maximize score using verified, exact, legal move sequences while preserving a robust submission pipeline.

Required architecture:
1. Start from the known-good exact lookup / deterministic replay baseline.
2. Add bounded exact local optimization only when every rewrite candidate is replay-verified through official CayleyPy move algebra.
3. Maintain a best-candidate portfolio per puzzle row: exact baseline, normalized commuting-order variants, bounded local DP rewrites, and small-support commutator/conjugate candidates.
4. Select the shortest verified legal sequence per row. If no improvement verifies, keep the verified baseline sequence.
5. Emit a compile-clean, importable solver module and a valid `submission.csv`.

Hard failure rules:
- Do not copy sample_submission.csv, emit `UNSOLVED`, emit empty rows, or rely on offline/sample fallback artifacts.
- Do not mark provider errors, prompt failures, or baseline fallback recovery as success.
- Do not emit an ambitious rewrite that breaks import, stdout JSON, or CSV generation.
- All final moves must be legal official move names and replay-equivalent for the target row.

Implementation preference:
Use small, staged patches with explicit rollback guards. Keep output deterministic. Keep stdout JSON machine-parseable. Use conservative exact improvements that survive strict self-checks instead of speculative optimizers.


## Success gate and anti-fallback requirements (added 2026-04-26)
- A round is successful only when you generate a real, importable solver script plus a valid non-empty Kaggle submission CSV. Fallback artifacts, copied `sample_submission.csv`, offline baseline placeholders, `UNSOLVED` rows, empty move strings, or provider-failure recovery outputs are failed attempts and must not be described as success.
- Keep the known-good exact lookup / exact replay path first. Then add only rollback-safe improvements: bounded exact local rewrites, deterministic candidate portfolios, semantic replay guards, and score-regression checks.
- Do not trade correctness for ambition. If a heuristic cannot be replay-verified on the official CayleyPy move algebra, reject it and keep the best verified candidate for that row.
- Preserve the required script contract: official legal moves only, deterministic output, compile/import cleanly, generate `submission.csv`, and keep stdout JSON parseable for the sweep pipeline.
- Never emit code that silently falls back to sample submissions, bundled baseline CSVs, or placeholder `UNSOLVED` records. If generation is uncertain, produce the smallest compile-valid solver with exact lookup and verified local optimizer rather than a broken large rewrite.

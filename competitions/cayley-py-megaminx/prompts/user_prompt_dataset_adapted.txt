We are solving the Kaggle competition "CayleyPy Megaminx Solve Optimally":
https://www.kaggle.com/competitions/cayley-py-megaminx

I need a Python module `solve_module.py` that preserves the exact bundled-state lookup pipeline and upgrades the path optimizer using ideas adapted from several inspected Megaminx datasets.

PRIMARY GOAL
Produce the best bundled-data score you can while keeping the solver fully deterministic, standard-library only, and competition-contract safe.

ABSOLUTE REQUIREMENTS
- Start from the working bundled-data baseline rather than replacing it with a risky generic solver.
- Preserve `solve(vec) -> (moves, sorted_array)`.
- Preserve script mode:
  `python solve_module.py "[1,2,3]"`
  -> `{"moves": [...], "sorted_array": [...]}`
- Use ONLY official move names from `puzzle_info.json`.
- Do NOT invent moves or rename generators internally.
- Returned `sorted_array` must be the actual state obtained by replaying the returned moves.
- Keep exact bundled lookup compatibility with:
  - `optimized_lookup.json` if present,
  - bundled optimized submission if present,
  - otherwise deterministic rebuilding from `sample_submission.csv`.

COMPETITION CONTEXT
- State vectors have length 120.
- The solved state is `central_state` from `puzzle_info.json`.
- All legal moves are order-5 Megaminx face turns and their inverses from `puzzle_info.json`.
- Submission paths are dot-separated move names.
- The Kaggle score is the total number of moves across all 1001 test states.
- `sample_submission.csv` is valid but strongly non-optimal, so exact path rewriting over the provided valid paths is a correct strategy.

DATASET-ADAPTED IDEAS TO USE
1. From meet-in-the-middle / hash-table Megaminx solvers:
   - use a stronger fixed-depth exact local effect table;
   - use deterministic lookup of shortest short words;
   - keep the precomputation radius constant.

2. From pruning-table / coordinate-based last-layer solvers:
   - prefer compact cached representations of local move effects;
   - avoid recomputing composed permutations repeatedly;
   - prune obvious inverse backtracking while building short exact tables.

3. From dodecahedron cycle-based simulators:
   - respect the true order-5 face structure;
   - derive commutation only from the official permutations in `puzzle_info.json`;
   - use that commutation graph for canonicalization.

RECOMMENDED IMPROVEMENTS
- fixed-radius exact short-word table at depth 5 if practical;
- stronger commuting normalization using the official generator commutation graph;
- bounded-window dynamic programming over local segments;
- multiple optimization passes until convergence or a small fixed pass limit;
- caching of permutation bytes and exact short words;
- preserve exact bundled validity first, then improve path compression.

FORBIDDEN APPROACHES
- replacing the bundled-data solver with a generic heuristic that can fail on bundled rows;
- BFS / DFS / A* / IDA* / beam search whose depth grows with the instance;
- whole-state-space search;
- returning `UNSOLVED` for bundled rows that the baseline solves;
- non-deterministic or provider-dependent behaviour.

IMPLEMENTATION STYLE
- Keep the code readable and well-factored.
- Standard library only.
- Prefer exact local equivalence replacement over speculative heuristics.
- Preserve backwards compatibility with the existing competition folder layout.

We are solving the Kaggle competition "CayleyPy Megaminx Solve Optimally":
https://www.kaggle.com/competitions/cayley-py-megaminx

I need a Python module `solve_module.py` that improves an already working Megaminx solver while preserving full compatibility with the official competition bundle.

ABSOLUTE REQUIREMENTS
- Start from the baseline code below. It already produces a valid bundled-data submission.
- Use ONLY the official competition move names from `puzzle_info.json`.
- Do NOT rename generators internally.
- Do NOT invent moves.
- Moves are applied left-to-right exactly as in the competition.
- Preserve the API `solve(vec) -> (moves, sorted_array)`.
- `sorted_array` must be the actual final state obtained after applying the returned moves.
- When run as a script:
  `python solve_module.py "[1,2,3]"`
  it must print JSON: `{"moves": [...], "sorted_array": [...]}`

COMPETITION CONTEXT YOU MUST USE
- The Megaminx competition state is a permutation vector of length 120.
- The solved state is the central state from `puzzle_info.json`.
- Legal generators are the Megaminx face turns and their inverses from `puzzle_info.json`.
- Submission paths are dot-separated generator names.
- The Kaggle score is the sum of move counts over all 1001 test states.
- `sample_submission.csv` is valid but highly non-optimal, so improving existing valid paths is a correct and important direction.

IMPORTANT SOLVER DESIGN RULES
- Preserve the current exact-match bundled lookup behavior. If the input vector matches a bundled test state, return a valid path for that exact state.
- The baseline already has an optimized bundled lookup pipeline. Do not throw that away.
- Prefer deterministic polynomial-time optimizations over generic exponential search.
- Small fixed-radius exact optimization is allowed, because the radius is constant and does not grow with input size.
- Whole-state-space search, IDA*, A*, BFS, DFS, beam search over growing depth, or brute force over full states are forbidden.


VERY IMPORTANT
- Do NOT replace the current approach with a generic heuristic that can return invalid or unsolved outputs for bundled states.
- Do NOT return `UNSOLVED` for bundled competition rows if the baseline can solve them.
- Prefer exact bundled lookup first, then safe deterministic fallback only if necessary.
- Keep the implementation self-contained and standard-library only.



Output requirements for your answer
- Return the full improved `solve_module.py` in exactly one ```python ...``` block.
- Do not include any text outside that code block.
- The produced code must be self-contained and runnable.

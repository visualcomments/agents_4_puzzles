"""Local llm-puzzles support package.

This file intentionally exists to make ``import src`` resolve to the bundled
repository package deterministically, instead of falling back to an unrelated
namespace/site-package called ``src`` that may exist in the Python environment.
"""
